[package]
name = "test-html-ut"
description = "HTML output unit tests."
version = "0.1.0"
edition = "2021"

[dependencies]
jotdown = { path = "../..", features = ["log"] }
env_logger = "0.11"

[lib]
name = "test_html_ut"
path = "lib.rs"
